# Definindo a função apra calcular a área do retângulo
# def calcular_area_retagulo (base, altura):
#     area = base * altura
#     return area

base = float(input("Qual é a base: "))
altura = float(input("Qual altura do retangulo? "))

area = base * altura

print(f"O total do triangulo é {area}")